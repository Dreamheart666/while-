#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
int main()
{
	int x, y;
	
	while (1)//判断语句，1为真，一直执行
	{
		scanf("%d", &x);
		if (x < 1)
			y = x;
		if (x >= 1 && x < 10)
			y = 2 * x - 1;
		if (x >= 10)
			y = 3 * x - 11;
		printf("y=%d\n", y);
	}
	
	return 0;
}
